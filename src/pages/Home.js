import CoinsTable from '../components/CoinsTable/CoinsTable';
import Hero from '../components/Hero';

const Home = () => {
	return (
		<>
			<Hero />
			<CoinsTable />
		</>
	);
};

export default Home;
