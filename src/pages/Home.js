import CoinsTable from '../components/CoinsTable/CoinsTable';
import Hero from '../components/Hero/Hero';

const Home = () => {
	return (
		<>
			<Hero />
			<CoinsTable />
		</>
	);
};

export default Home;
